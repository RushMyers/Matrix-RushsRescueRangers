import { adopters } from './stores/adopters.store';
import { animals } from './stores/animals.store';
import { appState } from './stores/appState.store';

export const APP_STORES = {
    adopters,
    animals,
    appState
};
